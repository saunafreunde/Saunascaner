import { Gliders } from '@/components/Gliders';

// Thin wrapper — die Segelflieger bleiben 1:1, sind die einzige durchgehende
// atmosphärische Bewegung der Basis.
export function StageGliders() {
  return <Gliders />;
}
